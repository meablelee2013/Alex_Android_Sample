pluginManagement {
    repositories {
        // 本地仓库
        mavenLocal()
        maven {
            url 'https://jitpack.io'
            allowInsecureProtocol = true
        }
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal()

        google()
        mavenCentral()
    }
}

rootProject.name = "Alex_Android_Sample"
include ':app'
